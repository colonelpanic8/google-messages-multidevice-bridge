{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The bundled setup page and the remote bridge client may use the app commands",
  "windows": ["main"],
  "remote": {
    "urls": ["http://*/*", "https://*/*"]
  },
  "permissions": ["core:default"]
}
