{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The bundled setup page may use the app commands. The bridge origin is granted the same access at runtime, once it is known.",
  "windows": ["main"],
  "permissions": ["core:default", "allow-app-commands"]
}
