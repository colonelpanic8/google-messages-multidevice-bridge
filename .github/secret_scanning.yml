# This vendored file exactly matches the pinned mautrix/gmessages source and
# contains the Google Messages client API key that upstream intentionally ships.
paths-ignore:
  - "third_party/mautrix-gmessages/pkg/libgm/util/constants.go"
